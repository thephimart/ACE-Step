# ACE-Step XPU Requirements
#
# This is the COMPLETE requirements file for XPU users.
#
# IMPORTANT: This is DIFFERENT from requirements.txt
# XPU users have a separate installation path.
#
# Installation Instructions:
#
# Step 1: Install XPU packages (with --pre)
# pip install --pre -r requirements-xpu-part1.txt --index-url https://download.pytorch.org/whl/nightly/xpu
#
# Step 2: Install missing packages from PyPI
# pip install -r requirements-xpu-part2.txt
#
# Step 3: Install ACE-Step
# pip install -e ".[xpu]"
#
# For complete installation script, see: INSTALL_XPU.md

# ========================================
# Part 1: Packages from XPU Index
# ========================================
# These packages are available in PyTorch XPU nightly index
# Install with: pip install --pre -r requirements-xpu-part1.txt --index-url https://download.pytorch.org/whl/nightly/xpu

torch
torchvision
torchaudio
transformers
tokenizers
datasets
